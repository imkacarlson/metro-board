# Copy to secrets.py on the CIRCUITPY drive and fill in.
# secrets.py is gitignored and is never part of an OTA manifest.
#
# This file is required by the Matrix Portal library to properly initialize the
# WiFi chip on the board.
secrets = {
	'ssid': 'your_wifi_network_name',
	'password': 'your_wifi_password',
	'timezone': 'America/New_York'
}
